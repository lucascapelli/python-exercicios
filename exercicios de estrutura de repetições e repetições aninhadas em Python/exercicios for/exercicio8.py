#Mostre a tabuada do 8 (ou seja, 8 × 1 até 8 × 10), usando um for.

for i in range(1, 11):
    print(f"8 x {i} = {8 * i}")

# Passamos pelos números de 1 a 10.
# A cada repetição, multiplicamos o número atual por 8.
# Isso gera a tabuada do 8 no formato tradicional (8 x 1 = 8, 8 x 2 = 16, ...).
